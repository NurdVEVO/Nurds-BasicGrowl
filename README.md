[README.md](https://github.com/user-attachments/files/29251831/README.md)
# Nurds Basic Growl

IR seeker growl audio mod for Nuclear Option.

## Install

Also available on NOMM

1. Delete Or back up any old install for this mod from `Nuclear Option\BepInEx\plugins`.
2. Extract `com.basicGrowl-1.0.1.zip` directly into `Nuclear Option\BepInEx\plugins`.
3. Check that this file exists:

   `Nuclear Option\BepInEx\plugins\Nurds_Basic_growl\com.basicGrowl.dll`

Do not extract the zip inside an existing mod folder.

Older builds may have used one of these folder names: `BasicGrowl`, `Basic Growl`, `basicGrowl`, `com.basicGrowl`, `SoundOfTheGrowlers`, or `SoundsOfTheGrowlers`. Delete those old folders before installing this release.

The `AAM1.txt`, `AAM3.txt`, and `SAM_IR1.txt` files next to the DLL are editable profiles. The `SeekerNoises` folder contains the wav files used by those profiles.
